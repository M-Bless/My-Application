<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".CrampingActivity"
            android:exported="false" />
        <activity
            android:name=".ExaminationActivity"
            android:exported="false" />
        <activity
            android:name=".ExerciseActivity"
            android:exported="false" />
        <activity
            android:name=".OvulationActivity"
            android:exported="false" />
        <activity
            android:name=".PregnancyActivity"
            android:exported="false" />
        <activity
            android:name=".MenstruationActivity"
            android:exported="false" /> <!-- Main Activity (Login Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Sign-Up Activity -->
        <activity android:name=".SignUpActivity" /> <!-- Dashboard Activity -->
        <activity android:name=".DashboardActivity" />
    </application>

</manifest>